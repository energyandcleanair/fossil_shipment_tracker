# pytest.ini
[pytest]
minversion = 6.0
addopts =
    -m 'system'
python_files =
    test_*.py
markers =
    system: marks a test as a full system test
    integration: marks a test as an integration test
